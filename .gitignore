node_modules
dist
dist-ssr
*.local
.DS_Store

# Local MCP secrets (SERPER_API_KEY, etc.)
.env
.env.*
!.env.example
.mcp.local.json
.claude/settings.local.json

# Playwright
test-results
playwright-report
shots

# Generated by plugins/site.ts from content/. Every page is written to its
# URL path at the repo root so Vite emits it at that path in dist/.
/index.html
/work
/logician-ui
/harness
/about
/blog
/public/sitemap.xml
/public/robots.txt

# Storybook
storybook-static
